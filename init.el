;; disable scroll bar on startup
(toggle-scroll-bar -1)

;; theme
(load-theme 'misterioso t)

;; sentence punctuation
(setq sentence-end-double-space nil)

;; add new lines with ctrl-n if at end of buffer
(setq next-line-add-newlines t)
